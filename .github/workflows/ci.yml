name: CI

on: [push, pull_request]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run smoke test
        run: |
          chmod +x ./tests/smoke_test.sh
          ./tests/smoke_test.sh
